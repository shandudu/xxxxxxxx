[plugin]
summary = "MES bill of materials management"
version = "0.1.0"
description = "Versioned BOM headers, components, validation and material requirement calculation"
author = "fba"
tags = ["other"]
database = ["mysql", "postgresql"]
depends_on = ["material"]

[app]
extend = "admin"

[api.bom]
prefix = "/bom"
tags = "MES BOM management"
