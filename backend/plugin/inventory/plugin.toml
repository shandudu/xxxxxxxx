[plugin]
summary = "MES inventory balances, ledger and stock movements"
version = "0.1.0"
description = "Authoritative inventory balances, immutable stock ledger, adjustments and location transfers"
author = "fba"
tags = ["storage", "other"]
database = ["mysql", "postgresql"]
depends_on = ["material", "warehouse", "trace"]

[app]
extend = "admin"

[api.inventory]
prefix = "/inventory"
tags = "MES inventory"
